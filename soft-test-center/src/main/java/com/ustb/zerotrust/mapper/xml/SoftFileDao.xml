<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ustb.zerotrust.mapper.SoftFileDao">
    <select id="findAll" resultType="SoftFile">
        select * from t_softfile
    </select>

</mapper>